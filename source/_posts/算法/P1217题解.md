---
title: "【题解】P1217 [USACO1.5] 回文质数"
date: 2025-03-06 00:13:44
updated: 2026-01-25 03:53:31
categories:
  - "算法"
  - "题解"
---

# P1217 [USACO1.5] 回文质数（[洛谷题面](https://www.luogu.com.cn/problem/P1217)）

## 题目

题目描述：

因为 $151$ 既是一个质数又是一个回文数（从左到右和从右到左是看一样的），所以 $151$ 是回文质数。

写一个程序来找出范围 $[a,b] (5 \le a < b \le 100,000,000)$（一亿）间的所有回文质数。

输入格式：

第一行输入两个正整数 $a$ 和 $b$。

输出格式：

输出一个回文质数的列表，一行一个。

数据范围与说明：

Hint 1: Generate the palindromes and see if they are prime.

提示 1: 找出所有的回文数再判断它们是不是质数（素数）.

Hint 2: Generate palindromes by combining digits properly. You might need more than one of the loops like below.

提示 2: 要产生正确的回文数，你可能需要几个像下面这样的循环。

题目翻译来自NOCOW。

USACO Training Section 1.5

产生长度为 $5$ 的回文数：

```cpp
for (d1 = 1; d1 <= 9; d1+=2) {    // 只有奇数才会是素数
     for (d2 = 0; d2 <= 9; d2++) {
         for (d3 = 0; d3 <= 9; d3++) {
           palindrome = 10000*d1 + 1000*d2 +100*d3 + 10*d2 + d1;//(处理回文数...)
         }
     }
 }
```

输入输出样例 #1

输入：

```
5 500
```

输出：

```
5
7
11
101
131
151
181
191
313
353
373
383
```

## 代码

```cpp
#include<bits/stdc++.h>
using namespace std;
int n,m;
bool prime(int x) {
    if (x < 2) return false;
    if (x == 2) return true;
    if (x % 2 == 0) return false;
    for (int i = 3; i * i <= x; i += 2) {
        if (x % i == 0) return false;
    }
    return true;
}
bool check(int x){
    string ans;
    int s=x;
    while(s){
        ans.push_back(s%10+'0');
        s/=10;
    }
    int l=0,r=ans.size()-1;
    while(l<r){
        if(ans[l++]!=ans[r--]){
            return false;
        }
    }
    return true;
}
int main(){
    cin>>n>>m;
    if(m>=9999999) m=9999999;
if(n%2==0)n++;
    for(int i=n;i<=m;i++){
        if(check(i)&&prime(i)){
            cout<<i<<endl;
        }
    }
    return 0;
}
```
