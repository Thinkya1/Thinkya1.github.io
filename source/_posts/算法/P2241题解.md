---
title: "【题解】P2241"
date: 2025-02-21 18:34:40
updated: 2026-01-25 03:53:39
categories:
  - "算法"
  - "题解"
---

# P2241

## 题目

题目描述：

有一个 $n \times m$ 方格的棋盘，求其方格包含多少正方形、长方形（不包含正方形）。

输入格式：

一行，两个正整数 $n,m$（$n \leq 5000,m \leq 5000$）。

输出格式：

一行，两个正整数，分别表示方格包含多少正方形、长方形（不包含正方形）。

数据范围与说明：

输入输出样例 #1

输入：

```
2 3
```

输出：

```
8 10
```

## 代码

```cpp
#include<bits/stdc++.h>
using namespace std;
long long n,m,sqr,rec;
int main(){
    cin>>n>>m;
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            if(i==j)    sqr+=(n-i)*(m-j);
            else    rec+=(n-i)*(m-j);
        }
    }
    cout<<sqr<<" "<<rec<<endl;
    return 0;
}
```
